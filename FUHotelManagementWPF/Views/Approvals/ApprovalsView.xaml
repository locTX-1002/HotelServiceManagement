<UserControl x:Class="FUHotelManagementWPF.Views.Approvals.ApprovalsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <StackPanel>
                <TextBlock Text="Phê duyệt nghiệp vụ" Style="{StaticResource PageTitleText}" />
                <TextBlock Text="Kiểm tra yêu cầu của nhân viên trước khi thực hiện thao tác nhạy cảm."
                           Style="{StaticResource CaptionText}" Margin="0,5,0,0" />
            </StackPanel>
            <Button Content="Tải lại" Command="{Binding RefreshCommand}"
                    Style="{StaticResource GhostButton}" HorizontalAlignment="Right" />
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,20,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <DataGrid ItemsSource="{Binding Requests}" SelectedItem="{Binding SelectedRequest}"
                          AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="LOẠI YÊU CẦU" Binding="{Binding TypeText}" Width="180" />
                        <DataGridTextColumn Header="ĐỐI TƯỢNG" Binding="{Binding TargetText}" Width="100" />
                        <DataGridTextColumn Header="GIÁ TRỊ" Binding="{Binding ValueText}" Width="130" />
                        <DataGridTextColumn Header="NGƯỜI YÊU CẦU" Binding="{Binding RequesterText}" Width="180" />
                        <DataGridTextColumn Header="THỜI GIAN" Binding="{Binding RequestedAt, StringFormat=dd/MM/yyyy HH:mm}" Width="150" />
                        <DataGridTextColumn Header="LÝ DO" Binding="{Binding Reason}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
                <TextBlock Text="Không có yêu cầu đang chờ duyệt."
                           HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource BodyText}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right"
                            Margin="16,14">
                    <Button Content="Từ chối" Command="{Binding RejectCommand}"
                            IsEnabled="{Binding HasSelection}" Style="{StaticResource GhostButton}" />
                    <Button Content="Duyệt yêu cầu" Command="{Binding ApproveCommand}"
                            IsEnabled="{Binding HasSelection}" Style="{StaticResource PrimaryButton}"
                            Margin="10,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
