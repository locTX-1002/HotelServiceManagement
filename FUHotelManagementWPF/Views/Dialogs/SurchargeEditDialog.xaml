<Window x:Class="FUHotelManagementWPF.Views.Dialogs.SurchargeEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Style="{StaticResource BaseWindow}">
    <Border Style="{StaticResource Card}" Effect="{StaticResource SoftShadowLarge}"
            Margin="16" Padding="26,22">
        <StackPanel>
            <TextBlock Text="{Binding Title}" Style="{StaticResource SectionTitleText}" />

            <TextBlock Text="LOẠI PHỤ THU" Style="{StaticResource FieldLabel}" Margin="0,18,0,5" />
            <ComboBox ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                      IsEnabled="{Binding CanSelectItem}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding Name, Mode=OneWay}" FontWeight="SemiBold" />
                            <Run Text=" · " />
                            <Run Text="{Binding UnitPrice, Mode=OneWay, StringFormat={}{0:N0}}" />
                            <Run Text=" đ/" />
                            <Run Text="{Binding Unit, Mode=OneWay}" />
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Text="SỐ LƯỢNG" Style="{StaticResource FieldLabel}" Margin="0,14,0,5" />
            <TextBox Text="{Binding QuantityText, UpdateSourceTrigger=PropertyChanged,
                                    ValidatesOnNotifyDataErrors=True}" />

            <Border Margin="0,14,0,0" CornerRadius="8" Padding="10,8"
                    Background="{StaticResource DangerSoftBrush}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                           TextWrapping="Wrap" />
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Huỷ" Style="{StaticResource GhostButton}" IsCancel="True" />
                <Button Content="Lưu" Style="{StaticResource PrimaryButton}"
                        Margin="10,0,0,0" MinWidth="96" IsDefault="True"
                        Command="{Binding SaveCommand}" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
