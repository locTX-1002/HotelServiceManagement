<Window x:Class="FUHotelManagementWPF.Views.Dialogs.PaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ghi nhận thanh toán"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Style="{StaticResource BaseWindow}">
    <Border Style="{StaticResource Card}" Effect="{StaticResource SoftShadowLarge}"
            Margin="16" Padding="26,22">
        <StackPanel>
            <TextBlock Text="Ghi nhận thanh toán" Style="{StaticResource SectionTitleText}" />
            <TextBlock Text="Dư nợ hiện tại" Style="{StaticResource FieldLabel}" Margin="0,18,0,5" />
            <TextBlock Text="{Binding RemainingText}" Style="{StaticResource PageTitleText}"
                       Foreground="{StaticResource BrandBrush}" />

            <TextBlock Text="SỐ TIỀN" Style="{StaticResource FieldLabel}" Margin="0,18,0,5" />
            <TextBox Text="{Binding AmountText, UpdateSourceTrigger=PropertyChanged,
                                    ValidatesOnNotifyDataErrors=True}" />

            <TextBlock Text="PHƯƠNG THỨC" Style="{StaticResource FieldLabel}" Margin="0,14,0,5" />
            <ComboBox ItemsSource="{Binding PaymentMethods}"
                      SelectedItem="{Binding SelectedMethod}"
                      DisplayMemberPath="Label" />

            <TextBlock Text="MÃ GIAO DỊCH" Style="{StaticResource FieldLabel}" Margin="0,14,0,5" />
            <TextBox Text="{Binding TransactionId, UpdateSourceTrigger=PropertyChanged,
                                    ValidatesOnNotifyDataErrors=True}"
                     IsEnabled="{Binding RequiresTransactionId}" />
            <TextBlock Text="Chỉ bắt buộc khi thanh toán bằng chuyển khoản."
                       Style="{StaticResource CaptionText}" Margin="0,5,0,0" />

            <Border Margin="0,14,0,0" CornerRadius="8" Padding="10,8"
                    Background="{StaticResource DangerSoftBrush}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                           TextWrapping="Wrap" />
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Huỷ" Style="{StaticResource GhostButton}" IsCancel="True" />
                <Button Content="Xác nhận" Style="{StaticResource PrimaryButton}"
                        Margin="10,0,0,0" MinWidth="110" IsDefault="True"
                        Command="{Binding SaveCommand}" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
