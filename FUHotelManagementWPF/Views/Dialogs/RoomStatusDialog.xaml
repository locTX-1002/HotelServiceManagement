<Window x:Class="FUHotelManagementWPF.Views.Dialogs.RoomStatusDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đổi trạng thái phòng"
        Width="410" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Style="{StaticResource BaseWindow}">
    <StackPanel Margin="24,20">

        <TextBlock Text="{Binding RoomTitle}" Style="{StaticResource SectionTitleText}" FontSize="18" />
        <TextBlock Style="{StaticResource CaptionText}" FontSize="12.5" Margin="0,4,0,14">
            <Run Text="Trạng thái hiện tại: " />
            <Run Text="{Binding CurrentStatusText, Mode=OneWay}" FontWeight="SemiBold" />
        </TextBlock>

        <!-- Cac buoc chuyen hop le -->
        <ListBox ItemsSource="{Binding Options}"
                 SelectedItem="{Binding SelectedOption}"
                 DisplayMemberPath="Label"
                 BorderThickness="0" Background="Transparent"
                 Visibility="{Binding HasOptions, Converter={StaticResource BoolToVis}}" />

        <!-- Khong co buoc chuyen nao -->
        <Border CornerRadius="8" Padding="12,10" Background="{StaticResource InfoSoftBrush}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasOptions}" Value="False">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding NoOptionMessage}" Foreground="{StaticResource InfoBrush}"
                       TextWrapping="Wrap" MaxWidth="330" />
        </Border>

        <Border Margin="0,12,0,0" CornerRadius="8" Padding="10,8"
                Background="{StaticResource DangerSoftBrush}">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                       TextWrapping="Wrap" MaxWidth="330" />
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Đóng" Style="{StaticResource GhostButton}" IsCancel="True" />
            <Button Content="Xác nhận" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0"
                    MinWidth="110" IsDefault="True"
                    Command="{Binding ConfirmCommand}"
                    Visibility="{Binding HasOptions, Converter={StaticResource BoolToVis}}" />
        </StackPanel>
    </StackPanel>
</Window>
