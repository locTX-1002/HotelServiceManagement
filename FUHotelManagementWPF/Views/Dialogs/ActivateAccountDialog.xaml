<Window x:Class="FUHotelManagementWPF.Views.Dialogs.ActivateAccountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Style="{StaticResource BaseWindow}">
    <Border Style="{StaticResource Card}" Effect="{StaticResource SoftShadowLarge}"
            Margin="16" Padding="26,22">
        <StackPanel>
            <TextBlock Text="{Binding Title}" Style="{StaticResource SectionTitleText}" />
            <TextBlock Style="{StaticResource CaptionText}" Margin="0,4,0,0" TextWrapping="Wrap">
                <Run Text="Khách sẽ đăng nhập ứng dụng bằng số điện thoại " /><Run Text="{Binding PhoneNumber, Mode=OneWay}" FontWeight="SemiBold" /><Run Text=" và mật khẩu đặt dưới đây." />
            </TextBlock>

            <TextBlock Text="MẬT KHẨU" Style="{StaticResource FieldLabel}" Margin="0,16,0,0" />
            <PasswordBox x:Name="PasswordInput" Style="{StaticResource InputPasswordBox}" />

            <TextBlock Text="XÁC NHẬN MẬT KHẨU" Style="{StaticResource FieldLabel}" Margin="0,14,0,0" />
            <PasswordBox x:Name="ConfirmInput" Style="{StaticResource InputPasswordBox}" />

            <TextBlock Text="Ít nhất 8 ký tự, gồm chữ hoa, chữ thường và chữ số."
                       Style="{StaticResource CaptionText}" Margin="0,6,0,0" />

            <!-- Banner loi (nhap thieu / policy tu service) -->
            <Border Margin="0,14,0,0" CornerRadius="8" Padding="10,8"
                    Background="{StaticResource DangerSoftBrush}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                           TextWrapping="Wrap" MaxWidth="330" />
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Huỷ" Style="{StaticResource GhostButton}" IsCancel="True" />
                <Button Content="Kích hoạt" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0"
                        MinWidth="110" IsDefault="True"
                        IsEnabled="{Binding CanSave}" Click="Activate_Click" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
