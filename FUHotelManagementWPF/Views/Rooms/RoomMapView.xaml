<UserControl x:Class="FUHotelManagementWPF.Views.Rooms.RoomMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">
    <Grid Margin="0,14,0,0">
        <!-- Dang tai -->
        <hc:LoadingCircle Width="34" Height="34"
                          Foreground="{StaticResource BrandBrush}"
                          Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

        <!-- Trong -->
        <TextBlock Text="Chưa có phòng nào đang hoạt động — thêm phòng ở tab Danh sách phòng."
                   Style="{StaticResource CaptionText}" FontSize="13"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}" />

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Chu giai mau -->
                <StackPanel Orientation="Horizontal" Margin="2,0,0,4">
                    <TextBlock Text="Bấm vào phòng để đổi trạng thái vận hành (dọn phòng / bảo trì)."
                               Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
                </StackPanel>

                <ItemsControl ItemsSource="{Binding Floors}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,6,0,10">
                                <TextBlock Text="{Binding FloorTitle}"
                                           Style="{StaticResource SectionTitleText}"
                                           Margin="2,0,0,8" />
                                <ItemsControl ItemsSource="{Binding Rooms}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource Card}" Width="152"
                                                    Padding="13,11" Margin="0,0,10,10"
                                                    Cursor="Hand"
                                                    ToolTip="Bấm để đổi trạng thái vận hành">
                                                <Border.InputBindings>
                                                    <MouseBinding Gesture="LeftClick"
                                                                  Command="{Binding DataContext.ChangeStatusCommand,
                                                                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                  CommandParameter="{Binding}" />
                                                </Border.InputBindings>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Room.RoomNumber}"
                                                               FontSize="17" FontWeight="SemiBold" />
                                                    <TextBlock Text="{Binding TypeName}"
                                                               Style="{StaticResource CaptionText}"
                                                               Margin="0,2,0,9" TextTrimming="CharacterEllipsis" />
                                                    <Border Style="{StaticResource RoomStatusBadge}">
                                                        <TextBlock Text="{Binding StatusText}" />
                                                    </Border>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
